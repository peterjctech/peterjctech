import React from "react";
import { Logo } from "components";

const Navbar = () => {
    return (
        <nav className="navbar">
            <Logo />
            <h1>Navbar</h1>
        </nav>
    );
};

export default Navbar;
